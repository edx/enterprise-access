name: Django CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
        django-version: ["pinned", "5.2"]
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        architecture: x64
    - name: Install requirements
      run: make requirements
    - name: Upgrade packages
      run: |
        pip install -U pip wheel -c requirements/constraints.txt
        if [[ "${{ matrix.django-version }}" != "pinned" ]]; then
          pip install "django~=${{ matrix.django-version }}.0"
          pip check # fail if this test-reqs/Django combination is broken
        fi
    - name: Validate translations
      run: make validate_translations
    - name: Run tests
      run: make test
    - name: Upload coverage
      if: matrix.django-version == 'pinned'
      uses: codecov/codecov-action@v6
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: false
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        architecture: x64
    - name: Install requirements
      run: make requirements
    - name: Upgrade packages
      run: pip install -U pip wheel
    - name: Run pylint
      run: pylint --rcfile=pylintrc enterprise_access/ *.py -v --ignore-paths="enterprise_access/apps/customer_billing/models.py,enterprise_access/apps/customer_billing/admin.py,enterprise_access/apps/api/v1/tests/test_checkout_bff_views.py"
    - name: Run pycodestyle
      run: make style
    - name: Run isort
      run: make isort_check
    - name: Run pii check
      run: make pii_check
    - name: Run check keywords
      run: make check_keywords
